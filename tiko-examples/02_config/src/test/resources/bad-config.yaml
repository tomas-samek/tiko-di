# Deliberately malformed fixture for ConfigurationAnchoredErrorsTest.
# Anchors expected:
#   - "db.url" MISSING_KEY      -> anchored to the "db" section header
#   - "app.server.port"         -> anchored to the "app.server" section header
#     INVALID_VALUE                (nested-record coercion limitation; see test Javadoc)
#   - "garbage" UNKNOWN_SECTION -> anchored to the section's own line
db:
  maxConnections: 5
  connectTimeout: PT3S
app:
  name: anchored-error-fixture
  logLevel: INFO
  server:
    host: 0.0.0.0
    port: not-a-number
garbage:
  key: value
