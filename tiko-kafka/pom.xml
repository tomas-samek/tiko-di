<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.tiko</groupId>
        <artifactId>tiko-parent</artifactId>
        <version>0.1.0</version>
    </parent>

    <artifactId>tiko-kafka</artifactId>
    <name>Tiko Kafka Transport</name>
    <description>
        Kafka transport module for Tiko DI — first concrete instance of the
        universal transport-adapter pattern. Ships @KafkaSource/@KafkaSink
        annotations, KafkaContext, the KafkaSerializer SPI, JSON serializer impl,
        Apache Kafka client wrapper, and the FakeKafkaBroker test helper.
    </description>

    <properties>
        <!-- Jackson lives under a relocated package after shading; keep the un-relocated
             scope to compile so source code uses the standard package names. -->
        <jackson.relocate.target>io.tiko.kafka.internal.jackson</jackson.relocate.target>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.tiko</groupId>
            <artifactId>tiko-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.tiko</groupId>
            <artifactId>tiko-config</artifactId>
        </dependency>

        <!-- Kafka client — exposed to users (they may pass it to bridge return types) -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
        </dependency>

        <!-- JSON serializer (shadow-bundled below; declared here for compile scope) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.tiko</groupId>
            <artifactId>tiko-runtime</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- tiko-processor runs on test sources to emit TikoContainerImpl + EventRegistry -->
        <dependency>
            <groupId>io.tiko</groupId>
            <artifactId>tiko-processor</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>4.2.2</version>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers — available to both unit and integration test classpaths -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>kafka</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${maven-shade.version}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <createDependencyReducedPom>true</createDependencyReducedPom>
                            <artifactSet>
                                <includes>
                                    <include>com.fasterxml.jackson.core:*</include>
                                    <include>com.fasterxml.jackson.datatype:*</include>
                                </includes>
                            </artifactSet>
                            <relocations>
                                <relocation>
                                    <pattern>com.fasterxml.jackson</pattern>
                                    <shadedPattern>${jackson.relocate.target}</shadedPattern>
                                </relocation>
                            </relocations>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>integTest</id>
            <build>
                <plugins>
                    <!-- Add src/integTest/java as an additional test source root -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <version>3.6.0</version>
                        <executions>
                            <execution>
                                <id>add-integtest-sources</id>
                                <phase>generate-test-sources</phase>
                                <goals>
                                    <goal>add-test-source</goal>
                                </goals>
                                <configuration>
                                    <sources>
                                        <source>src/integTest/java</source>
                                    </sources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- Override compiler for the test-compile execution only.
                         Applying this to default-compile would re-process src/main/java's
                         @Configuration KafkaConfig and clash with the hand-maintained
                         KafkaConfigBinder shipped at src/main/java/io/tiko/generated/config. -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-testCompile</id>
                                <configuration>
                                    <source>17</source>
                                    <target>17</target>
                                    <release>17</release>
                                    <proc>full</proc>
                                    <annotationProcessorPaths>
                                        <path>
                                            <groupId>io.tiko</groupId>
                                            <artifactId>tiko-processor</artifactId>
                                            <version>${project.version}</version>
                                        </path>
                                        <path>
                                            <groupId>io.tiko</groupId>
                                            <artifactId>tiko-kafka-processor</artifactId>
                                            <version>${project.version}</version>
                                        </path>
                                    </annotationProcessorPaths>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- Run *IT.java classes via Failsafe -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>3.2.2</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>**/*IT.java</include>
                                    </includes>
                                    <!-- docker-java 3.4.0 (pulled by Testcontainers 1.20.4) defaults to
                                         API 1.32, which Docker Desktop 25+ rejects. Pin a modern API
                                         version supported by current engines. -->
                                    <systemPropertyVariables>
                                        <api.version>1.43</api.version>
                                    </systemPropertyVariables>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
